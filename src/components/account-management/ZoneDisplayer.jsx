/**
 * This component simply displays the zones on account. This could be a preview,
 * the zones currently on the account, etc.
 */
function ZoneDisplayer( {zonesToDisplay} )
{
    return(
        <div>
            
        </div>
    );
}

export default ZoneDisplayer;