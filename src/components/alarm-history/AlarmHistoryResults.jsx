/**
 * A component that manages displaying alarm history for a passed account.
 * @param {*} account 
 * @param {*} startDate 
 * @param {*} endDate 
 * @returns 
 */
function AlarmHistoryResults(account, startDate, endDate)
{
    function updateDisplayedAlarms()
    {
        
    }
    // This is where checking some sort of database will occur
    return(
        <div>

            {/* The container holding the results */}
            <div>

            </div>

        </div>
    );
}

export default AlarmHistoryResults;